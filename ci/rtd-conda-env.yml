# https://conda.io/docs/using/envs.html#create-environment-file-by-hand
name: rtd-conda-env
dependencies:
- python=3.5
# For some reason RTD's default install of this is v0.1.7, which is ancient
- rtd_sphinx_theme=0.1.9
